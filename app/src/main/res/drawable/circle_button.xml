<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@color/red"/> <!-- Change to your color -->
    <size
        android:width="5dp"
        android:height="5dp"
        android:radius="0.5mm"
        android:background="@color/red"

        />
</shape>
